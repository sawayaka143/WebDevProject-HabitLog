// todos-tab.ts — replaced by task-editor + terminal workflow
// This component is no longer used. Retained as an empty stub to prevent build errors.
import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-todos-tab',
  standalone: true,
  imports: [CommonModule],
  template: ``
})
export class TodosTabComponent {}
